#include "CLI.h"

int main()
{
    
    CLI *cli = new CLI;
   /* Parser * parser = new Parser;
    Execute execute();
    int cmdCount = 0;
    char * cmd;
    printf("----------------------HELLO TO MY SHELL------------------------\n");
   // while(1) 
  // {
       
        printf(">cmd >>> ");
        cli->getCommand();
        //parser.parseCommand(cli);
        //execute.run(parser);


    //}

    printf("----------------------END TO MY SHELL------------------------\n");
*/
    return 0;
}
